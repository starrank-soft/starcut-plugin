# StarCut for TRAE

Copy the `.agents` and `.trae` directories into the root of a TRAE
project. If the project already has `.trae/mcp.json`, merge the
`mcpServers.starcut` entry instead of replacing the file. Open TRAE's MCP
settings, authorize StarCut, and start a new Agent conversation.
